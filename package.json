{
  "name": "Graph.js",
  "version": "0.1.0",
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  },
  "devDependencies": {
    "karma": ">=0.8"
  },
  "engines": {
    "node": ">=0.8"
  }
}